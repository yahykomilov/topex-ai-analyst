# Topex AI Analyst — задачи

Обновлено: 2026-08-04. Новый этап: внутренние роли (оператор / РОП / директор),
логин-пароль на каждого, статистика по ролям, апгрейд ИИ.

## Статус — что уже сделано
- [x] Ядро: amoCRM → расшифровка → оценка ИИ → Telegram + PDF, 2 языка (узб/рус).
- [x] Статистика: недозвоны вынесены в отдельную категорию (📵), больше не «провал»;
      проценты успех/неуспех/сомнение считаются от **отвеченных** звонков.
- [x] Каркас ролей — модуль `auth.py`: логин/пароль (pbkdf2+соль), филиалы,
      вход/выход с привязкой Telegram, видимость по ролям. Самотест `_test_auth.py` проходит.
      **В `main.py` пока НЕ вшит** — следующий шаг дорожки A.
- Решение по ИИ: **OpenAI пока оставляем**, Claude не включаем. Переход на «без OpenAI +
      платный ИИ» — когда клиент оплатит (правка небольшая).

## Дорожки (делёж)

### A. Доступ + сервер  — [Я]
Оператор видит только своё · РОП — всех операторов своего филиала · директор — сводку по 3 филиалам.
- [ ] Вшить `auth.py` в `main.py`: `/login`, `/logout`; после входа бот сразу удаляет
      сообщение с паролем (чтоб не висело в чате).
- [ ] Команды владельца: завести филиал · завести пользователя · привязать оператора
      к его amoCRM-id.
- [ ] Ре-скоуп ВСЕХ экранов по роли смотрящего (меню, список сотрудников, статистика, отчёты).
- [ ] Завести 3 филиала и раскидать операторов.
- [ ] Деплой на сервер (Ubuntu, python 3.12, `ffmpeg`), автозапуск — переживает падение и
      перезагрузку; логи в файл. `.env` кладём на сервер **вручную**, не через git.
- [ ] Перевыпустить ВСЕ утёкшие ключи (бот, amoCRM, Groq, Gemini, OpenAI) — светились в чатах.

### B. ИИ  — [Отабек]
- [ ] Определиться с моделью для оценки разговора (обсудить отдельно).
- [ ] Платный тариф на ИИ, чтоб под нагрузкой не упираться в лимиты.
- [ ] В разборе показывать, ГДЕ именно в звонке ошибка (место в диалоге).
- [ ] Показывать диалог прямо в Telegram, не только в PDF.
- [ ] (по команде) Убрать OpenAI, когда клиент оплатит.

### C. Статистика по ролям  — [Дони]
- [ ] Экран директора — сводка по 3 филиалам (где лучше/хуже).
- [ ] Экран РОПа — все операторы его филиала: рейтинг, записи, ошибки.
- [ ] Экран оператора — только свои звонки, оценка, где ошибся.
- [ ] Дневной отчёт с учётом роли смотрящего.

### D. Тексты (узб+рус) + тесты  — [Абдулазиз]
- [ ] Тексты всех новых экранов (вход/пароль, «нет доступа», отчёты, роли) — на узбекском и русском.
- [ ] E2E-прогон на 11 операторах Topex: имена, статусы (разговор/недозвон), отсечка коротких,
      качество узбекской речи, дневной отчёт в 20:00.
- [ ] Проверить, что распознавание узбекского (Gemini) и разделение говорящих держатся.

## Безопасность (важно)
`.env` с реальными ключами уже светился в чатах — считать ВСЕ ключи скомпрометированными и
перевыпустить после деплоя (дорожка A). Секреты — только в `.env` на сервере,
НИКОГДА в чат/репозиторий (`.env` уже в `.gitignore`).

## Изучить аналоги
- [ ] Deepsales, myROP — что перенять под наш формат.
