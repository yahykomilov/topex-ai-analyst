# Topex AI Analyst — задачи

Обновлено: 2026-08-05 (сверка после пуша Абдулазиза + мерджа веток команды).

## Статус — что уже сделано
- [x] Ядро: amoCRM → расшифровка → оценка ИИ → Telegram + PDF.
- [x] 3 языка интерфейса: русский / узбекский / английский (переключатель 🇷🇺/🇺🇿/🇬🇧),
      полная симметрия ключей проверена (`_test_roles.py` + ручная сверка).
- [x] Расшифровка: Gemini 2.5-flash первым для узбекского (разделяет говорящих —
      Operator/Mijoz), Whisper (Groq) — страховка.
- [x] Анализ: OpenAI `gpt-4o-mini` основной → Groq `llama-3.3-70b` → Groq `llama-3.1-8b`
      фолбэк-цепочка с авто-переключением при лимите/ошибке (Отабек, `7ef4109`).
- [x] В разборе — 📍 ГДЕ именно ошибка (дословная цитата реплики менеджера + часть
      разговора), плюс полный разбор (диалог+ошибки+оценка) шлётся прямо в Telegram,
      не только в PDF (Отабек, `7ef4109`).
- [x] Статистика: недозвоны — отдельная категория (📵), не «провал»; % успех/неуспех/
      сомнение считаются от **отвеченных** звонков.
- [x] Роли — `auth.py` вшит в `main.py`: `/login /logout /whoami`, пароль удаляется из
      чата после ввода; оператор видит только своё, РОП — свой филиал, директор/
      владелец — сводку по всем филиалам (`db.stats_for_ids`, `stats_by_branch`).
      Команды владельца: `/addbranch /branches /adduser /users`.
- [x] `render.yaml` — конфиг для деплоя на Render (worker, env через дашборд, без
      секретов в репо).

## Сделано ещё (2026-08-05, вторая волна — «добей сам, Дони не делает»)
- [x] **Дыры в доступе закрыты** (коммит `a2ef6a2`): `cb_call`/`cb_analyze` не проверяли
      роль вообще — залогиненный оператор мог открыть/запустить разбор чужого звонка
      по id. Добавлены те же проверки, что у соседних хендлеров.
- [x] **Отчёт за день — по роли смотрящего.** Раньше кнопка была видна всем, но
      РОП/оператору отвечала «нет доступа» (тупиковая UX). Теперь `build_daily_report`
      скоупится через `visible_manager_ids`, доступен любому вошедшему; автоотчёт в
      20:00 уходит не только владельцу, а каждому вошедшему пользователю — его версией.
- [x] **Заведён 1 филиал + реальные люди** — амоCRM-id подтянуты напрямую из API:
      РОП Bobur Xolov (login `bobur`) + 5 реально звонящих операторов (`durdona`,
      `iroda`, `ruxshona`, `medine`, `sarvinoz`). Логины/пароли — у Yahyo в личке.
      ⚠️ Это ОДИН placeholder-филиал на всех — реального деления на несколько филиалов
      я не знаю (в amoCRM один ROP на всех), если оно есть — нужно от Yahyo.
      Остальные 6 amoCRM-юзеров (Uljanoy3, Sotuv4, Ozoda, Muxlisa, Muslima, Belfort) не
      заведены — по CRM у них нет ни одного звонка.
      Live-проверка через настоящий логин операторов НЕ сделана — все 4 «свои» Telegram
      (Yahyo+3 EXTRA_OWNER_IDS) авто-считаются владельцем, обходят вход; нужен реальный
      человек (например Iroda со своего телефона), чтобы увидеть just-operator вид.

## Осталось реально

### A. Доступ + сервер
- [ ] Прогнать деплой по `render.yaml` (или другой хостинг) — учесть, что бот сейчас
      живёт локально и не переживает перезагрузку ПК.
- [ ] Перевыпустить ВСЕ ключи (бот-токен, amoCRM, Groq, Gemini, OpenAI) — светились в
      чатах несколько раз, включая пересылку `.env` в группу 2026-08-04/05.

### B. ИИ — качество и лимиты
- 🔴 **КРИТИЧНО (нашёл Абдулазиз на E2E 2026-08-05):** бесплатная квота Gemini —
  20 запросов/сутки, кончается уже на 3-м звонке из 5 тестовых. После — падает на
  Groq Whisper, а там **нет разделения говорящих** и узбекский текст рассыпается в
  кашу (не проходит нормально даже через _quality_check). При реальном объёме
  (100+ звонков/сутки по данным amoCRM) это бьёт по большинству звонков.
- 🔴 **OpenAI отдавал 429/413 практически на каждом запросе** и в тесте Абдулазиза, и
  живьём в `bot.log` сегодня — стоит проверить, реально ли на ключе есть оплаченный
  биллинг (сейчас похоже что нет / лимит нулевой), иначе цепочка «OpenAI основной»
  на практике всегда уезжает на Groq.
- [ ] Решить: платный Gemini (снимает лимит 20/день) и/или подтвердить биллинг OpenAI.
- [ ] Калибровка промпта — проверить, что оценка не занижена/завышена на живых звонках.

### C. Статистика по ролям
- [x] Экран директора/владельца — сводка по филиалам есть на уровне кода
      (`stats_by_branch`), но **не проверена вживую**, т.к. филиалов ещё нет (см. A).
- [ ] Экран РОПа/оператора — проверить на реальном пользователе после того, как
      появятся филиалы и хотя бы один РОП-аккаунт.

### D. Тексты + тесты — [Абдулазиз, готово]
- [x] Тексты всех экранов (вход/пароль/роли/«нет доступа») — уз/рус/англ.
- [x] E2E-прогон на реальных звонках 2026-08-05 (см. баг в Track B выше). Из 11
      операторов amoCRM реально звонят только 5 (Durdona, Iroda, Medine, Ruxshona,
      Sarvinoz) — совпадает с `MANAGER_WHITELIST`, у остальных 6 в CRM нет звонков.

## ⚠️ Важно на будущее — не поднимать два инстанса бота одновременно
2026-08-05 в 12:11 два процесса опрашивали Telegram с одним и тем же токеном
(похоже, локальный E2E-тест Абдулазиза пересёкся с уже работавшим у меня) →
`TelegramConflictError`, бот завис в бесконечном ретрае на несколько часов.
Для локальных тестов — либо согласовывать в группе кто сейчас гоняет прод-бота,
либо завести отдельный тестовый бот-токен через @BotFather.

## Безопасность (важно, повторно)
`.env` с реальными ключами уже светился в чатах МИНИМУМ дважды (31.07, 04-05.08).
Считать ВСЕ ключи скомпрометированными и перевыпустить при деплое (A). Секреты —
только в `.env`/переменных хостинга, никогда в чат или репозиторий.

## Изучить аналоги
- [ ] Deepsales, myROP — что перенять под наш формат.
