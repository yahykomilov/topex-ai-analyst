# Topex AI Analyst — задачи

Обновлено: 2026-08-04. Новый этап: внутренние роли (оператор / РОП / директор),
логин-пароль на каждого, статистика по ролям, апгрейд ИИ.

## Статус — что уже сделано
- [x] Ядро: amoCRM → расшифровка → оценка ИИ → Telegram + PDF, 2 языка (узб/рус).
- [x] Статистика: недозвоны вынесены в отдельную категорию (📵), больше не «провал»;
      проценты успех/неуспех/сомнение считаются от **отвеченных** звонков.
- [x] Каркас ролей — модуль `auth.py`: логин/пароль (pbkdf2+соль), филиалы,
      вход/выход с привязкой Telegram, видимость по ролям. Самотест `_test_auth.py` проходит.
      **В `main.py` пока НЕ вшит** — следующий шаг дорожки A.
- [x] Тексты для логина/пароля/«нет доступа»/ролей (уз+рус+англ) — черновик готов
      в `i18n.py` (ключи `auth_*`, `role_*`). Когда `auth.py` вошьётся в `main.py`,
      просто вызывайте `t("auth_login_prompt")` и т.д. — если формулировки не подойдут
      под реальный флоу, поправим на месте.
- Решение по ИИ: **OpenAI пока оставляем**, Claude не включаем. Переход на «без OpenAI +
      платный ИИ» — когда клиент оплатит (правка небольшая).

## Дорожки (делёж)

### A. Доступ + сервер  — [Я]
Оператор видит только своё · РОП — всех операторов своего филиала · директор — сводку по 3 филиалам.
- [x] Вшить `auth.py` в `main.py`: `/login`, `/logout`; после входа бот сразу удаляет
      сообщение с паролем (чтоб не висело в чате). — сделано в дорожке C.
- [x] Команды владельца: завести филиал · завести пользователя · привязать оператора
      к его amoCRM-id (`/branch_add`, `/user_add`, `/bind`, `/amo_ids`). — сделано в дорожке C.
- [x] Ре-скоуп ВСЕХ экранов по роли смотрящего (меню, список сотрудников, статистика,
      отчёты, карточки звонков). — сделано в дорожке C.
- [ ] Завести 3 филиала и раскидать операторов.
- [ ] Деплой на сервер (Ubuntu, python 3.12, `ffmpeg`), автозапуск — переживает падение и
      перезагрузку; логи в файл. `.env` кладём на сервер **вручную**, не через git.
- [ ] Перевыпустить ВСЕ утёкшие ключи (бот, amoCRM, Groq, Gemini, OpenAI) — светились в чатах.

### B. ИИ  — [Отабек]
- [ ] Определиться с моделью для оценки разговора (обсудить отдельно).
- [ ] Платный тариф на ИИ, чтоб под нагрузкой не упираться в лимиты.
- [ ] В разборе показывать, ГДЕ именно в звонке ошибка (место в диалоге).
- [ ] Показывать диалог прямо в Telegram, не только в PDF.
- [ ] (по команде) Убрать OpenAI, когда клиент оплатит.

### C. Статистика по ролям  — [Дони]
- [x] Экран директора — сводка по филиалам (сколько операторов, звонков, %% и средний
      балл по каждому + «лучше всех / слабее всех»), провал в филиал → в оператора.
- [x] Экран РОПа — операторы его филиала: рейтинг по среднему баллу, звонки, ошибки.
- [x] Экран оператора — только свои звонки, своя оценка, свои ошибки.
- [x] Дневной отчёт с учётом роли: директор — весь отдел, РОП — свой филиал,
      оператор — только себя. Автоотчёт в 20:00 рассылается каждому по его роли
      (операторам — выключено флагом `DAILY_AUTO_TO_OPERATORS` в `main.py`, чтобы не
      плодить запросы к ИИ; свой отчёт оператор берёт кнопкой).
- [x] Ре-скоуп всех экранов звонков: карточка сотрудника, даты, день, разбор, аудио,
      текст, PDF — чужое не открывается даже по прямой кнопке (`auth.can_see_manager`).
- Тесты: `_test_roles.py` (данные/доступ) и `_test_screens.py` (тексты и кнопки самих
      экранов, бот не запускается и в сеть не ходит). Оба зелёные.
- ⚠️ Попутно пришлось закрыть зависимость из дорожки A (без входа роли не работают):
      `auth.py` вшит в `main.py` — `/login`, `/logout`, `/whoami` + команды владельца
      `/branch_add`, `/branches`, `/user_add`, `/users`, `/bind`, `/passwd`,
      `/user_on`, `/user_off`, `/amo_ids`. Сообщение с паролем бот удаляет сразу.
      Осталось по A: завести 3 реальных филиала и раскидать операторов, деплой, ключи.

### D. Тексты (узб+рус) + тесты  — [Абдулазиз]
- [x] Тексты всех новых экранов (вход/пароль, «нет доступа», отчёты, роли) — на узбекском,
      русском и английском. Черновик в `i18n.py` (`auth_*`, `role_*`), готов к использованию
      как только `auth.py` вошьётся в `main.py`.
- [x] Пересверено 2026-08-05 после merge `origin/doni` в `main` (auth.py реально вшит):
      все ключи `t(...)`, вызываемые в `main.py`, присутствуют в `uz`/`ru`/`en` — пропусков нет
      (проверено скриптом по всем вызовам). Самотесты `_test_auth.py`/`_test_roles.py`/
      `_test_screens.py` — все ALL PASSED. Один неиспользуемый ключ: `auth_already_logged`
      (текст на случай повторного /login при уже активной сессии — в `main.py` такая проверка
      пока не реализована, это код-логика Track A, не текст). Устаревшие комментарии
      «черновик, ждёт вшивания auth.py» в `i18n.py` обновлены.
- [x] E2E-прогон на реальных звонках (2026-08-05, локально, в Telegram ничего не отправлялось):
      имена/статусы определяются верно, недозвон отдельно от провала, PDF собирается.
      Из 11 операторов amoCRM реально звонят только **5** (Durdona, Iroda, Medine, Ruxshona,
      Sarvinoz) — совпадает с текущим `MANAGER_WHITELIST`; у остальных 6 (Uljanoy3, Sotuv 4,
      Ozoda Komiljonovna, Muxlisa, Muslima, Belfort) в CRM нет ни одного звонка.
      Дневной отчёт в 20:00 **не проверялся** (не запускал live-поллер, чтобы не задеть
      боевого бота).
- [x] Распознавание узбекского (Gemini) и разделение говорящих — см. баг ниже, держатся
      только пока не кончится бесплатная квота Gemini.

**🔴 БАГ (найден 2026-08-05, критично для Track B):** бесплатный тариф Gemini — 20
запросов/сутки. На 5 тестовых звонках квота кончилась на 3-м (`429
RESOURCE_EXHAUSTED`), дальше падает на Groq Whisper. Пока держит Gemini — узбекский
текст чистый и есть разметка `Operator:`/`Mijoz:`. На Whisper-фолбэке — разметки
говорящих **нет вообще**, а узбекский текст рассыпается в кашу (примеры — расшифровки
Iroda/Durdona/Medine в `data/calls.db` локально, row_id 1/4/5). При ~100+ звонков/сутки
(по данным amoCRM) это бьёт по большинству реальных звонков. Заодно: OpenAI (должен
быть основным провайдером) отдавал 429 на **каждом** запросе — по факту анализ и
расшифровка сейчас всегда уезжают на Groq. Разобраться в Track B/A (тариф Gemini/OpenAI).

## Безопасность (важно)
`.env` с реальными ключами уже светился в чатах — считать ВСЕ ключи скомпрометированными и
перевыпустить после деплоя (дорожка A). Секреты — только в `.env` на сервере,
НИКОГДА в чат/репозиторий (`.env` уже в `.gitignore`).

## А3 — Подключить платный ИИ (OpenAI GPT) вместо Groq

Проблема: бот сейчас на бесплатном Groq (whisper + llama) — лимиты и качество
средние. Решение: OpenAI GPT становится основным провайдером, Groq остаётся
бесплатным fallback'ом. Код уже готов (правка в `transcriber.py`, 2026-08-01).

Покупаем (https://platform.openai.com — нужна международная Visa/MC):
- Расшифровка: `gpt-4o-mini-transcribe` — **$0.003/мин**
- Анализ:     `gpt-4o-mini` — **$0.15 / $0.60 за 1M токенов**

Цена в месяц (звонок ~5 мин ≈ $0.016):
- 500 звонков/мес   → **~$8/мес**
- 1 000 звонков/мес → **~$16/мес**
- Пополнение **$55** хватает на ~3 400 звонков ≈ 2–3 месяца работы.

Шаги:
1. platform.openai.com → Billing → пополнить **$55**.
2. API keys → Create new secret key.
3. Ключ вписать в `.env` **вручную** (НЕ в чат/репо! секреты — только на сервере):
   ```
   OPENAI_API_KEY=sk-...
   OPENAI_TRANSCRIBE_MODEL=gpt-4o-mini-transcribe
   ANALYSIS_MODEL=gpt-4o-mini
   ```
4. Перезапустить бот.
5. Проверить по логам: «OpenAI Transcribe» вместо «Groq Whisper».

Опционально (качество выше, цена выше):
- `OPENAI_TRANSCRIBE_MODEL=gpt-4o-transcribe` ($0.006/мин) — лучше узбекский.
- Анализ через Claude: `ANTHROPIC_API_KEY` + `CLAUDE_MODEL` (код в `analyzer.py` готов).

## Г1 — End-to-end тест на 11 операторах Topex (#9)

Цель: вся цепочка работает на реальных данных без ручного вмешательства.

Шаги:
1. Прогнать полный путь: звонок в amoCRM → скачивание записи (onlinePBX) →
   распознавание → оценка LLM → PDF → отправка в бот.
2. Проверить на всех 11 операторах: имена, статусы (разговор/недозвон),
   отсечка коротких звонков, качество узбекской речи.
3. Баги → отдельный список → раздать по владельцам модулей → починить →
   прогнать заново до «зелёного».
4. Проверить дневной отчёт в 20:00 и общую статистику.

## Интеграция веток команды

- [x] Отабек `xcrpty7` (#3/#4/#5) — влито в `main` 2026-07-30
      (промпт-чеклист, мульти-провайдер STT + `preprocess.py`, статусы, багфикс py3.12).
- [ ] Дони + Абдулазиз (В: PDF/языки/поиск) — влить, когда запушат ветку.
- Процесс: смотрим ветку → быстрый ревью → merge в `main` → `git pull` локально.

## Безопасность — ПРОВЕРИТЬ 2026-07-31

В группу «Проекты» скинули файл `.env` с реальными ключами. Это утечка.
1. Забрать/удалить файл из группы.
2. Считать ВСЕ ключи скомпрометированными → перевыпустить (см. А2):
   бот-токен, amoCRM, Groq, Gemini, OpenAI.
3. Донести команде: секреты — только в `.env` на сервере, НИКОГДА в чат/репо.

## Готово (подготовка)

- [x] Репо + структура (база Дони), 9 задач заведены как Issues.
- [x] Делёж по дорожкам А/Б/В/Г, таски расписаны в группе по людям.
- [x] Доступ: Отабек принял инвайт; Дони (переотправлен) и Абдулазиз — ждут.
- [x] Локальная рабочая копия синхронна с `main`.

## Изучить аналоги
- [ ] Deepsales, myROP — что перенять под наш формат.
